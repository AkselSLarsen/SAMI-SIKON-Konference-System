@page
@model SAMI_SIKON.Pages.Login.RegisterPageModel
@{
}


<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.User.Email" class="custom-control-label"></label>
                <input asp-for="@Model.User.Email" class="form-control" />
                <span asp-validation-for="@Model.User.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.User.Name" class="custom-control-label"></label>
                <input asp-for="@Model.User.Name" class="form-control" />
                <span asp-validation-for="@Model.User.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.User.PhoneNumber" class="custom-control-label"></label>
                <input asp-for="@Model.User.PhoneNumber" class="form-control" />
                <span asp-validation-for="@Model.User.PhoneNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.User.Password" class="custom-control-label"></label>
                <input asp-for="@Model.User.Password" class="form-control" />
                <span asp-validation-for="@Model.User.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
@if (Model.CreationMessage.StartsWith("Success"))
{
    <div class="alert alert-info alert-primary">
        @Model.CreationMessage
    </div>
}

@if (Model.CreationMessage.StartsWith("Error"))
{
    <div class="alert alert-danger">
        @Model.CreationMessage
    </div>
}